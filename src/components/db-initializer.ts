import '../db/init';

// This is just a dummy component that imports the initializer
// The actual initialization happens in the imported file
export default function DbInitializer() {
  return null;
} 